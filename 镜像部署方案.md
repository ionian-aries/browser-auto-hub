# 镜像部署方案

> Browser Auto Hub 前后端镜像的构建、安装、环境配置与运维说明。
> 部署拓扑（spec 1 十八次修订）：**MySQL / MinIO 为外部已 provisioning 设施**，
> 镜像仅含 backend（FastAPI + 调度器 + 浏览器引擎）与 frontend（nginx + 静态页）。

## 1. 镜像构成

| 镜像 | 基础 | 大小 | 内容 |
|------|------|------|------|
| `browser-auto-hub-backend:latest` | python:3.11-slim | ~2.75GB | uv workspace（backend+engine 同进程）、Playwright Chromium、中文字体（文泉驿正黑）、Node 18 |
| `browser-auto-hub-frontend:latest` | nginx:alpine | ~94MB | 前端构建产物 + 反代配置（`/api` → `backend:8900`，支持 SSE/WebSocket 长连） |

两镜像均由仓库根 `docker-compose.yml` 编排，Dockerfile 在 `docker/` 目录。

## 2. 前置条件

1. **Docker**（含 compose 插件）
2. **外部 MySQL 8.0**：库表须提前创建——按 `数据库脚本/` 目录 01、02 执行
  （平台启动不做 create_all，缺表即报错；详见 `数据库脚本/数据库脚本说明.md`）
3. **外部 MinIO**：bucket 提前创建（默认 `browser-auto-hub`），平台仅连接不建桶
4. 部署机网络可达：MySQL、MinIO、以及被自动化的目标站点（如 OA）

## 3. 构建镜像

仓库根目录执行：

```bash
docker compose build            # 构建全部
docker compose build backend    # 只构建某一个
```

构建是离线可重复的：`uv.lock` / `pnpm-lock.yaml` 冻结依赖；backend 镜像构建期执行
`playwright install chromium` 下载浏览器（构建机需能访问 Playwright CDN）。

## 4. 环境配置

配置唯一来源是 env 文件，两个现成模板：

| 文件 | 用途 |
|------|------|
| `.env` | 宿主机开发（backend 连 127.0.0.1 的 MySQL，端口 8900/3200） |
| `.env.docker` | **容器部署**（MySQL 走 `host.docker.internal`，端口避开宿主机 dev，8901/3201） |

变量说明：

| 变量 | 作用 | 注意 |
|------|------|------|
| `DATABASE_URL` | MySQL 连接串 | 容器内 `127.0.0.1` 指容器自身；连宿主机/外部 MySQL 用 `host.docker.internal`（compose 已配 host-gateway，Linux 同样有效），且 MySQL 账号需授权 docker 网段（如 `'root'@'%'`） |
| `MINIO_ENDPOINT` / `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY` | MinIO 连接 | endpoint 必须**容器内可达** |
| `MINIO_BUCKET` / `MINIO_OBJECT_PREFIX` | 桶名 / 对象键前缀 | bucket 须已存在；前缀完全决定附件存储路径（默认 `attachments`） |
| `PUBLIC_BASE_URL` | 平台对外地址 | **必须与 `BACKEND_PORT` 实际映射一致且消费方可达**——pipeline 用它拼接附件下载 URL（`/api/files/{key}`）；写错则附件链接打不开 |
| `BACKEND_PORT` / `FRONTEND_PORT` | 宿主机映射端口 | compose 变量替换用，容器内端口恒为 8900/80 |
| `ENV_FILE` | 服务 env_file 指令解析 | `.env.docker` 内已自指（`ENV_FILE=.env.docker`），下方启动命令无需另设 shell 变量 |

## 5. 启动 / 停止 / 更新

```bash
# 启动（构建后直接起）
docker compose --env-file .env.docker up -d

# 代码更新后：重新构建并替换容器（数据在外部 MySQL/MinIO，不受影响）
docker compose build && docker compose --env-file .env.docker up -d

# 查看状态 / 日志
docker compose ps
docker compose logs -f backend

# 停止 / 删除容器（不动外部数据）
docker compose down
```

## 6. 验证

```bash
# 后端直连接口（宿主机映射端口）
curl http://localhost:8901/api/pipelines

# 前端页面（nginx 反代 /api 到 backend 容器）
open http://localhost:3201
```

`GET /api/pipelines` 返回两条 OA 流水线（`oa.communicate_todos` /
`oa.communicate_forward`）即正常——它们是后端启动时自动同步进库的。

## 7. 初始化顺序（全新环境）

1. MySQL：执行 `数据库脚本/01`、`02` 建库建表
2. MinIO：创建 bucket
3. 启动平台（§5），后端自动同步 pipelines 注册表
4. 执行 `数据库脚本/03` 写入「每 10 分钟采集待办」默认调度（依赖第 3 步）
5. 填入真实 OA 凭据后启用该调度（见 `数据库脚本/数据库脚本说明.md`）

## 7.1 快速上手：HTTP 交互示例（容器端口 8901）

均为异步：返回 `201` + `id`，轮询 `GET /api/executions/{id}` 至终态。
完整参数说明见 `OA流水线API触发指南.md` 与 `OA流水线CURL调用手册.md`
（示例为 dev 端口 8900，容器部署用 8901）。

```bash
# ① 主动触发 todos（沟通待办采集）
curl -X POST http://localhost:8901/api/executions \
  -H "Content-Type: application/json" \
  -d '{"pipeline":"oa.communicate_todos","trigger_type":"api",
       "config":{"username":"OA账号","password":"OA密码"}}'

# ② 主动触发 forward（沟通批量转发）
curl -X POST http://localhost:8901/api/executions \
  -H "Content-Type: application/json" \
  -d '{"pipeline":"oa.communicate_forward","trigger_type":"api",
       "config":{"username":"OA账号","password":"OA密码",
         "forwards":[{"task_id":"待办fdId","recipients":["张三"]}]}}'

# ③ 每 10 分钟自动采集待办：先取 pipeline id，再建调度
curl -s http://localhost:8901/api/pipelines     # 找 oa.communicate_todos 的 id
curl -X POST http://localhost:8901/api/schedules \
  -H "Content-Type: application/json" \
  -d '{"pipeline_id":"上一步的id","name":"每10分钟采集待办",
       "trigger_type":"interval","interval_seconds":600,
       "config_override":{"username":"OA账号","password":"OA密码"}}'
```

## 8. 注意事项

- **附件 URL 可达性**：消费方拿到的是 `{PUBLIC_BASE_URL}/api/files/{key}` 代理地址，
  永不过期、无需可达 MinIO；但要求 `PUBLIC_BASE_URL` 对消费方真实可达
- **浏览器资源**：backend 容器 `shm_size: 2gb`（Chromium 共享内存，勿删）；
  单容器内浏览器串行使用，无需额外 sandbox 参数
- **API 两种访问路径**：前端经 nginx 反代（`FRONTEND_PORT`）；外部系统直连 backend
  （`BACKEND_PORT`）。两者等价，SSE 日志流均已放行（`proxy_buffering off`）
- **时区**：库中时间统一 UTC 存储；调度 cron/interval 首次触发时机见
  `OA流水线API触发指南.md` §4
- **重启行为**：`restart: unless-stopped`；后端启动时重载全部启用中的调度，
  interval 锚点从重启时刻重新计算
- **不要在容器内放状态**：执行记录、日志、业务数据全在外部 MySQL/MinIO，
  容器可随时销毁重建
- **CORS**：缺省 `*`（开发便利）；生产环境在 env 中加 `CORS_ORIGINS=https://你的域名`
