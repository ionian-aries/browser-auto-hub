[project]
name = "browser-auto-hub"
version = "0.1.0"
description = "Browser automation management platform"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["packages/backend", "packages/engine"]

[tool.uv.sources]
backend = { workspace = true }
engine = { workspace = true }
