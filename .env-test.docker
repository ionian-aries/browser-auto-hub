# Browser Auto Hub — 部署配置（deploy.sh + docker compose 共用）

# 数据库：本机 MySQL himea1
DATABASE_URL=mysql+aiomysql://himea:mPcbJCLD1QjcHv22R0eASkD21nzC@mysql:3306/himea?charset=utf8mb4

# MinIO 对象存储（pipeline 业务附件）
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=rqZD2YLUhsGTdQZGlCfN8LjRlrky
MINIO_BUCKET=himea-skill-packages
MINIO_OBJECT_PREFIX=attachments

# 本机访问
PUBLIC_BASE_URL=http://localhost:8901

BACKEND_PORT=8901
FRONTEND_PORT=3201

TABLE_inbox_documents=skill_custom_inbox_documents
