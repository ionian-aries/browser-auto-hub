# OA 沟通待办采集 Pipeline 设计

> 日期：2026-07-21
> 状态：待实施
> 依赖：spec 1（平台架构）

---

## 1. 概述

将现有 `browser-agent/scripts_v3/oa_communicate_todos.py` 迁移为 browser-auto-hub 平台的第一个正式 Pipeline，支持定时/API/手动三种触发方式。

### 1.1 功能

- 登录 OA 系统（支持 login.jsp 直接登录和 IAM OAuth SSO）
- 遍历沟通待办列表（含分页）
- 逐条打开详情，提取元数据
- 下载附件并上传至 MinIO，生成预签名 URL
- 按 task_id 去重后写入 MySQL `inbox_documents` 表
- 全程通过 StepLogger 记录步骤日志 + 截图 + SSE 推送

### 1.2 非目标（本次不做）

- 转发功能（`fwd` 字段由后续 pipeline 处理）
- 并发采集（保持串行，避免 OA 限流）
- cookie 缓存/session 复用

---

## 2. 目录结构

```
packages/engine/src/engine/
├── pipelines/
│   └── oa/
│       ├── __init__.py
│       ├── shared/
│       │   ├── __init__.py
│       │   └── login.py              # OA 登录（支持 login.jsp + IAM SSO）
│       └── communicate_todos.py      # @register_pipeline 沟通待办采集
├── registry.py                        # PipelineRegistry + 递归自动发现
├── base.py                            # BasePipeline + PipelineMetadata + PipelineResult
├── runner.py                          # 统一运行入口 + ExecutionContext 组装
└── logger.py                          # StepLogger（DB 写入 + SSE 广播）
```

### 2.1 自动发现规则

`registry.py` 在应用启动时递归扫描 `engine/pipelines/` 目录下所有 `.py` 文件，导入每个模块。只有带 `@register_pipeline` 装饰器的类才会被注册到 registry。`shared/` 下的文件无装饰器，导入时不产生注册。

---

## 3. Pipeline 注册

```python
@register_pipeline(
    name="oa.communicate_todos",
    display_name="OA 沟通待办采集",
    description="采集 OA 系统沟通待办列表，提取元数据与附件，写入 inbox_documents 表",
    trigger_modes=["cron", "api", "manual"],
    config_schema={
        "type": "object",
        "properties": {
            "login_url": {
                "type": "string",
                "default": "https://ioa.sd-port.net/login.jsp",
                "description": "OA 登录页地址"
            },
            "username": {"type": "string", "description": "OA 用户名"},
            "password": {"type": "string", "description": "OA 密码"},
            "page_load_timeout": {
                "type": "integer",
                "default": 15000,
                "description": "页面加载超时(ms)"
            },
            "element_visible_timeout": {
                "type": "integer",
                "default": 5000,
                "description": "元素可见超时(ms)"
            },
            "action_settle_timeout": {
                "type": "integer",
                "default": 500,
                "description": "操作后稳定等待(ms)"
            }
        },
        "required": ["username", "password"]
    }
)
class OaCommunicateTodosPipeline(BasePipeline):
    async def execute(self, config: dict, ctx: ExecutionContext) -> PipelineResult:
        ...
```

---

## 4. 配置分层

### 4.1 平台配置（.env → pydantic-settings）

所有 pipeline 共享，由平台注入到 ExecutionContext：

| 配置项 | 说明 |
|--------|------|
| `database_url` | MySQL 连接字符串 |
| `minio_endpoint` | MinIO 端点 URL |
| `minio_access_key` | MinIO 访问密钥 |
| `minio_secret_key` | MinIO 密钥 |
| `minio_bucket` | MinIO 桶名 |
| `minio_object_prefix` | 对象键前缀 |
| `minio_presign_expires_seconds` | 预签名 URL 过期时间 |

### 4.2 Pipeline 运行参数（触发时传入 config）

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `username` | 是 | - | OA 用户名 |
| `password` | 是 | - | OA 密码 |
| `login_url` | 否 | `https://ioa.sd-port.net/login.jsp` | OA 登录页 |
| `page_load_timeout` | 否 | 15000 | 页面加载超时(ms) |
| `element_visible_timeout` | 否 | 5000 | 元素可见超时(ms) |
| `action_settle_timeout` | 否 | 500 | 操作后等待(ms) |

---

## 5. ExecutionContext

Runner 在调用 `pipeline.execute()` 前组装上下文，pipeline 无需自行管理基础设施连接：

```python
@dataclass
class ExecutionContext:
    logger: StepLogger          # 步骤日志（写 DB + SSE 广播）
    db: AsyncSession            # SQLAlchemy async session（平台 DB）
    minio: MinioClient          # MinIO 客户端（封装 boto3）
    settings: Settings          # 平台配置（readonly）
    execution_id: str           # 当前执行 ID（UUID v4）
```

---

## 6. 执行流程

```
触发 → runner.run(pipeline_name, config, execution_id)
│
├── 更新 task_executions.status = "running"
├── 实例化 OaCommunicateTodosPipeline
├── 构建 ExecutionContext
├── pipeline.execute(config, ctx):
│   │
│   ├── step: "launch_browser"
│   │   playwright.chromium.launch(headless=True)
│   │
│   ├── step: "login"
│   │   from engine.pipelines.oa.shared.login import oa_login
│   │   oa_login(page, config)
│   │
│   ├── step: "locate_list"
│   │   等待 portal_qdport.jsp iframe 就绪
│   │   检测空状态 → 若空则提前返回
│   │
│   ├── step: "crawl" (每页记录一次)
│   │   for each page:
│   │     for each title link:
│   │       打开详情标签页
│   │       提取元数据 (task_id, title, creator, send_time, etc.)
│   │       下载附件 → ctx.minio.upload → 生成预签名 URL
│   │       关闭详情标签页
│   │     翻页 (fingerprint 防死循环)
│   │
│   ├── step: "save"
│   │   过滤已存在 task_id (去重)
│   │   INSERT INTO inbox_documents (新记录)
│   │   通过 ctx.db 执行
│   │
│   └── 关闭浏览器
│
├── 更新 task_executions.status = "success" / "failed"
└── 记录 result_summary: {total, inserted, skipped, attachment_failures}
```

---

## 7. 数据库模型

### 7.1 inbox_documents 表

```sql
CREATE TABLE inbox_documents (
  id CHAR(36) NOT NULL COMMENT 'UUID v4',
  task_id VARCHAR(64) NOT NULL COMMENT 'OA 系统中的唯一标识 (fdId)',
  creator VARCHAR(255) DEFAULT NULL COMMENT '发起人',
  send_time VARCHAR(64) DEFAULT NULL COMMENT '发送时间',
  title TEXT NOT NULL COMMENT '标题',
  participants TEXT NOT NULL COMMENT '接收者（逗号分隔）',
  cc_recipients TEXT NOT NULL COMMENT '抄送人（逗号分隔）',
  summary TEXT COMMENT '正文摘要',
  attachment_urls TEXT NOT NULL COMMENT '附件预签名 URL（JSON 数组）',
  fwd TINYINT(1) DEFAULT 0 COMMENT '是否已转发: 0未转发 1已转发',
  skip TINYINT(1) DEFAULT 0 COMMENT '是否跳过: 0正常 1跳过',
  forward_time DATETIME DEFAULT NULL COMMENT '转发完成时间',
  PRIMARY KEY (id),
  UNIQUE KEY uk_task_id (task_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

### 7.2 去重策略

写入前通过 `SELECT task_id FROM inbox_documents` 加载已有 ID 集合，新记录中已存在的 task_id 直接跳过（不 INSERT）。使用 UNIQUE KEY 作为最终防线。

---

## 8. 触发方式

### 8.1 定时触发

通过前端创建 schedule：

```json
{
  "pipeline_id": "<oa.communicate_todos 的 ID>",
  "name": "工作日早8点采集OA待办",
  "trigger_type": "cron",
  "cron_expr": "0 8 * * 1-5",
  "config_override": {
    "username": "ceshiyong4",
    "password": "***"
  },
  "enabled": true
}
```

APScheduler 根据 cron 表达式到时间后触发 runner。

### 8.2 API 触发

```
POST /api/executions
{
  "pipeline": "oa.communicate_todos",
  "config": {
    "username": "ceshiyong4",
    "password": "***"
  }
}

Response 201:
{
  "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "pipeline": "oa.communicate_todos",
  "status": "pending",
  "created_at": "2026-07-21T08:00:00Z"
}
```

### 8.3 手动触发

前端 Pipeline 详情页 → 点击"立即执行" → 弹出配置表单（根据 config_schema 动态渲染 username/password 字段）→ 提交 → 调用同一 API。

---

## 9. OA 登录模块

### 9.1 位置

`packages/engine/src/engine/pipelines/oa/shared/login.py`

### 9.2 接口

```python
async def oa_login(page: Page, config: dict) -> None:
    """
    登录 OA 系统。

    支持两种入口：
    - 直接 login.jsp 表单
    - IAM OAuth SSO 页面

    Raises:
        LoginError: 凭据无效（不可重试）
        LoginTimeout: 页面/元素超时（可重试）
    """
```

### 9.3 依赖

仅依赖 `playwright.async_api.Page` + 标准库。不依赖平台设施（不需要 DB/MinIO）。

---

## 10. 浏览器策略

| 决策 | 选择 |
|------|------|
| 实例管理 | 每次执行启动全新实例；headless 与结束后是否关闭均为**可配置**（config.headless / config.close_browser，默认 true/true，可被全局运行设置与触发覆盖改变，见 spec 4 §8.2 九次修订） |
| 运行模式 | 默认 headless（生产）；headed 仅限本地调试（Docker 环境无 GUI） |
| Playwright 版本 | 使用平台 pyproject.toml 中统一管理的版本 |
| 浏览器类型 | Chromium（Playwright 内置） |

---

## 11. 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 浏览器管理 | 每次新实例 headless | 无状态、Docker 友好、避免 session 残留 |
| 登录 | 每次重新登录 | 简单可靠、无 cookie 管理开销 |
| 去重 | task_id UNIQUE + 预查询跳过 | 幂等、重复运行安全 |
| 数据存储 | 直接写 MySQL inbox_documents | 不产中间文件 |
| 公共逻辑 | oa/shared/ 目录 | OA 域共享、不参与注册扫描 |
| 附件 | 上传 MinIO → 预签名 URL | 复用平台 MinIO 配置 |
| DB 访问 | 通过 ExecutionContext 注入 AsyncSession | pipeline 无需自行管理连接 |
| MinIO 访问 | 通过 ExecutionContext 注入 | 统一配置、统一生命周期 |
| 采集方式 | 串行逐页逐条 | 避免 OA 系统限流 |
